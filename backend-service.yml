apiVersion: v1
kind: Service
metadata:
  name: call-center-service
  namespace: backend
spec:
  type: ClusterIP
  selector:
    app: call-center-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
